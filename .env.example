# xAI / Grok API key — https://console.x.ai
XAI_API_KEY=

# X (Twitter) API bearer token — https://developer.x.com
X_BEARER_TOKEN=

# Optional: path to a local clone of grok-install-ecosystem
# Used by the deploy bridge to call deploy_to_x from grok_install.runtime.
GROK_INSTALL_HOME=
